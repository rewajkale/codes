FirstMissingPositive:

public int firstMissingPositive(int[] nums) {
        Arrays.sort(nums);
        int count=1;
        int index=0;
        while(index>=0)
        {
        	index=Arrays.binarySearch(nums, count);
        	count++;
        }
        count--;
        return count;
    }
